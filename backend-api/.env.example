# Port for Express (Render sets PORT automatically)
PORT=3001

# Admin key for DELETE /api/messages/:id and /api/admin/verify
ADMIN_KEY=tadiwa-admin-2026

# Allowed frontend origins (comma-separated). Use * for local dev only.
CLIENT_ORIGIN=http://localhost:5173,http://127.0.0.1:5173
